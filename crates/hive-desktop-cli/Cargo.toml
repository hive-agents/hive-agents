[package]
name = "hive-desktop-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
hive-desktop-core = { path = "../hive-desktop-core" }
hive-protocol = { path = "../hive-protocol" }
serde_json = "1.0"
tokio = { version = "1.36.0", features = ["rt-multi-thread", "macros", "signal", "time"] }
